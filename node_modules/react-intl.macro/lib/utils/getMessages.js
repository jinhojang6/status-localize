"use strict";

exports.__esModule = true;
exports.default = getMessages;

var _astHelper = require("./ast-helper");

function getMessages(referencePath) {
  var properties = referencePath.parentPath.get('arguments.0.properties');
  var messages = properties.map(function (property) {
    return property.get('value');
  }).map(_astHelper.objectExpressionToObject);
  return messages;
}

module.exports = exports.default;