import { jsxAttributesToObject } from './ast-helper';
import { EXTRACT_JSX_ATTRIBUTES as ATTRIBUTES } from './constants';
export default function getJSXMessage(referencePath) {
  var attributes = referencePath.parentPath.get('attributes').filter(function (attribute) {
    return ATTRIBUTES.includes(attribute.get('name').node.name);
  });
  var message = jsxAttributesToObject(attributes);
  return message;
}