import { objectExpressionToObject } from './ast-helper';
export default function getMessages(referencePath) {
  var properties = referencePath.parentPath.get('arguments.0.properties');
  var messages = properties.map(function (property) {
    return property.get('value');
  }).map(objectExpressionToObject);
  return messages;
}